# Product

## Register

product

## Users

Simple Code 面向长期停留在终端中的 Python 与全栈开发者。他们在本地代码仓库中启动助手，以键盘完成提问、阅读模型输出和中断生成，不希望频繁切换窗口或被装饰性界面打断。核心任务是在尽可能低的交互成本下获得连续、可读、可信的多轮编程协助。

## Product Purpose

Simple Code 是一个终端原生 AI 编程助手。当前阶段负责稳定连接 Anthropic 与 OpenAI 兼容后端，在 TUI 中提供流式回答、会话记忆和 Extended Thinking 展示。成功意味着用户进入任意仓库后能立即辨认当前模型、工作目录和连接状态，提交消息后持续看到反馈，并能随时取消或继续下一轮对话。

## Brand Personality

克制、聪明、亲近。Simple Code 的品牌只体现在紧凑 ASCII 标识和雾紫点缀中，青绿色仅用于连接等积极状态，不牺牲专业感，不抢占代码与回答的注意力。文案短促、明确，状态表达像成熟开发工具而不是聊天娱乐平台。

## Anti-references

不采用网页聊天应用式的圆角气泡、卡片堆叠、大面积品牌色、渐变、玻璃拟态或装饰动画。不使用传统桌面应用式顶部导航，也不让 Header/Footer 占据过多终端高度。不复制 Claude 品牌文字，但参考 Claude Code 的终端原生密度、结构和安静视觉层级。

## Design Principles

1. 状态先于装饰：模型、目录、连接和生成状态必须一眼可见。
2. 输入始终是视觉锚点：边界清楚、键盘优先、生成期间反馈明确。
3. 内容保持终端原生：避免聊天气泡和多层容器，让文字自然流动。
4. 品牌只做识别：标识和紫色点缀形成记忆，不侵占工作区域。
5. 小终端仍可工作：在 `80×24` 下保留提交、取消、阅读和状态识别能力。

## Accessibility & Inclusion

界面使用高对比文本与不依赖颜色的状态文案，所有核心操作均可通过键盘完成。最小支持 `80×24` 终端；窄屏时优先保留输入和模型状态。避免装饰性动画，流式变化只表达真实生成进度；错误、取消和禁用状态必须有可读文字。
